import PageWrapper from "@/app/[locale]/components/page-wrapper";
import React from "react";

const AccountPage = () => {
  return <PageWrapper className="w-full h-full">AccountPage</PageWrapper>;
};

export default AccountPage;
